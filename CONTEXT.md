# STACK-AND-TRACK Context

## Mission

Help teams build plans that credibly add up to an outcome, learn from actual
results, and continuously redirect effort toward the highest remaining impact.

## Core loop

1. Define the baseline, target, unit, horizon, and measurement source.
2. Stack candidate interventions with predicted impact and confidence.
3. Remove overlap and expose dependencies.
4. Pareto-prioritize the smallest credible portfolio that can reach the target.
5. Prefer meaningful surplus coverage when feasible; 2x is a useful aspiration,
   not a universal physical requirement. Treat surplus as option, velocity, and
   regression reserve rather than permission for surplus concurrent work.
6. Track predicted and realized gains in the same unit.
7. Explain positive and negative variance.
8. Apply the learning to every remaining item.
9. Credit staged rollout gains only as the affected denominator is exposed.
10. Version target, denominator, horizon, or technology changes and re-audit
    transferable, stranded, and newly required work.
11. Re-stack until the outcome is reached or the target is explicitly revised.

## Non-negotiable principles

- A plan must add up before work begins.
- Activity is not impact.
- Realized gain outranks promised gain.
- Confidence, overlap, dependencies, and diminishing returns are explicit.
- Priorities change when evidence changes.
- Misses are learning inputs, not facts to hide.
- Bounded metrics require redundant paths, not impossible arithmetic.
- The method must work without custom software.

## Current scope

The repository defines the methodology, examples, templates, AI guidance, and
governance. It contains no substantive implementation code.

## Pause point

The documentation, simulations, transformed-composite examples, portable
schema suite, StackTrack CLI design, paper prototype, and synthetic `.roles`
reviews are complete enough for creator and independent-practitioner review.

The project is intentionally paused before implementation.

When work resumes:

1. ask David Blyth to review the public methodology, especially the README,
   canonical loop, 2x reserve doctrine, and StackTrack CLI boundaries;
2. run the independent-human self-service usability test;
3. run the two-cycle StackTrack Software Admission Test with at least three
   independent practitioners;
4. preserve findings and dissent in `context/role-reviews/`;
5. implement only the bounded read-only CLI slice if repeated evidence closes
   the Software Admission Gate.

Do not add calendar integration, hosted services, dashboards, mutation,
automatic prioritization, or event infrastructure before separate evidence and
role review.
